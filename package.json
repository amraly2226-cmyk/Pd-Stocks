{
  "name": "pd-stocks",
  "version": "1.0.0",
  "main": "Stocks-bot.js",
  "scripts": {
    "start": "node Stocks-bot.js",
    "build": "chmod +x render-build.sh && ./render-build.sh"
  },
  "dependencies": {
    "puppeteer": "^23.0.0"
  }
}
